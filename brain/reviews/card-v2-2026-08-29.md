# Review — PR #19: card-v2 per-pool+chain nullifier binding, zero-owner guard (audit #1/#6/#7)

- **Date:** 2026-08-29
- **Reviewer (independent session):** `v4flash-review-card-v2` (builder), report-only — never edits.
- **Implementer session:** `v4flash-implement-card-v2` (builder). Worktree `~/code/worktrees/s3ntiment-card-v2`, branch `deepseek/card-v2`.
- **Commit reviewed:** `77957f281` (`feat(S3ntimentSurveyStore): card-v2 per-pool+chain nullifier binding, zero-owner guard (audit #1/#6/#7)`)
- **Diff reviewed:** PR #19 against `origin/main` — 17 files (REPORT.md + 16 code/test). Reviewer judged the diff at `/tmp/cardv2.diff`, NOT the implementer's worktree.
- **Acceptance contract:** audit findings #1 + #6 + #7 ONLY; breaking card-format digest `keccak256(abi.encode(poolId, nullifier, batchId, address(this), block.chainid))`; per-pool nullifier scoping; zero-address owner guard; off-chain digest byte-identical to on-chain.

## Verdict: PASS — no blocking issues

## Checklist results

1. **Byte-identity (the crux) — CORRECT.** On-chain `abi.encode(poolId, nullifier, batchId, address(this), block.chainid)` ≡ off-chain `encodeAbiParameters(parseAbiParameters('string,string,address,address,uint256'), [poolId, nullifier, batchId, storeAddress, chainId])` — identical field order and dynamic-length prefixes. `abi.encode` (not `encodePacked`) is correct and required with two dynamic strings. `bigint` chainId ↔ uint256 correct; address case irrelevant. **Independent canary recomputed:** `(poolId="seam-pool-id", nullifier="A"+"a"*21, batchId=0x1111…, store=0xeeee…, chainId=8453n)` → `0xa01a0fb692be39ccb75d3f44b6e7cb45558cbda2717631ab6e0dafbc77d166d7` — matches the pinned seam constant exactly. EIP-191 wrapper byte-identical (both wrap exactly once).
2. **Cross-pool correctness** — per-pool `[poolId][messageHash]` keying + poolId-in-digest delivers independence; no residual cross-pool burn path found.
3. **Zero-owner guard** — revert `InvalidMemberAddress()` after burn but before membership write; rollback is sound; consistent with file error style.
4. **ABI completeness** — `isNullifierUsed` gains `string poolId`; every caller migrated in the diff (grep-verified, no stale 2-arg `signCardMessage`/`isNullifierUsed` remain); `registerInPool` external signature unchanged; new error `InvalidMemberAddress` in ABI artifact.
5. **Seam test** — canary recomputed to the new form; on-chain oracle round-trip in the seam verifies byte-identity end-to-end.
6. **Regression tests** — cross-pool redemption, per-pool independence, and zero-owner revert are present and assert the fixes.
7. **Scope discipline** — only #1/#6/#7 in the diff (no revokeBatch, no events, no Safe rotation, no pagination).
8. **No blocking issues found** in the full diff.

## Gates (independently reconciled by orchestrator)

- `contracts`: `cd contracts && pnpm exec hardhat test` → **50 passing** at committed HEAD (baseline 46), including the seam card-v2 oracle round-trip.
- Per implementer REPORT.md: shared 86 / organiser 28 / respondents 107, `check:abi` green (orchestrator reconciled the contracts gate; the seam passing inside it is the authoritative byte-identity proof).

## Disposition

PASS. PR #19 ready for human merge (orchestrator does not merge). After merge, handoff §7.1 should record card-v2 landed + the breaking card-format change.
